import React from 'react';

function Loading(Component) {
    // return (
    //     <div>
            if ({setLoading}) {
                return(
                   <p>Loading......!!</p> 
                )
                
            } else {
                
            }
    //     </div>
    // );
}

export default Loading;